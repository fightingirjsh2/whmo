---
import Base from "./Base.astro";

interface Props {
  title: string;
  description: string;
  date: Date;
  author: string;
  image?: string;
  backHref: string;
  backLabel: string;
  accentVar: string;
  feedLabel: string;
}

const { title, description, date, author, image, backHref, backLabel, accentVar, feedLabel } = Astro.props;
const dateStr = date.toLocaleDateString("en-US", { month: "long", day: "numeric", year: "numeric" });
---

<Base title={title} description={description} ogImage={image} ogType="article">
  {image && (
    <div class="article-hero">
      <img src={image} alt="" class="article-hero__img" />
    </div>
  )}
  <div class="container">
    <article class="article">
      <a class="back-link" href={backHref}>&#8592; {backLabel}</a>
      <p class="kicker article__kicker" style={`color: var(${accentVar})`}>{feedLabel}</p>
      <h1 class="article__title">{title}</h1>
      <p class="article__meta">{author} &middot; {dateStr}</p>
      <div class="article__body">
        <slot />
      </div>
    </article>
  </div>
</Base>
